============
Installation
============

At the command line::

    $ easy_install meta-stack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv meta-stack
    $ pip install meta-stack
